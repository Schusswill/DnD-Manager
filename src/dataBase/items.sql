create database dnd;
use dnd;
create table emp(id int(10), name varchar(40),age int(3));


create table items (
	id integer primary key,
	name Text,
	effect effects
	);

create table effects(
	id INTEGER PRIMARY KEY,
	);	